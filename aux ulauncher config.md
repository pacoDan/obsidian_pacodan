instalacion en arch:
```sh
git clone https://aur.archlinux.org/ulauncher.git && cd ulauncher && makepkg -is`
```
ubuntu:
```sh
sudo add-apt-repository universe -y && sudo add-apt-repository ppa:agornostal/ulauncher -y && sudo apt update && sudo apt install ulauncher
```
sino el .deb desde https://ulauncher.io/#Download:
```sh
sudo apt install ./ulauncher_x.y.z_all.deb
```

despues estas extensiones:
https://github.com/friday/ulauncher-clipboard
https://github.com/nourabosen/FileFlow
si la ultima no funciona, usar este: https://github.com/brpaz/ulauncher-file-search



---

